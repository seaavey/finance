/**
 * @deprecated This composable is superseded by the inline code in
 * src/pages/dashboard.vue. No page or component imports it.
 * Kept only as reference — will be removed once dashboard.vue is
 * refactored to use it.
 */
