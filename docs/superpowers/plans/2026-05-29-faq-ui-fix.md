# FAQ Accordion UI Fix Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix the FAQ UI by implementing a single rotating arrow icon in the Accordion component and cleaning up conflicting styles in the landing page.

**Architecture:** Refactor the base UI component (`AccordionTrigger.vue`) to handle its own state-based animation (rotation) and simplify its template. Update the consumer (`Faq.vue`) to remove manual overrides that cause double icons or alignment issues.

**Tech Stack:** Vue 3, Nuxt, Tailwind CSS, Hugeicons (Iconify), Reka UI (radix-vue).

---

### Task 1: Refactor AccordionTrigger Component

**Files:**
- Modify: `app/components/ui/accordion/AccordionTrigger.vue`

- [ ] **Step 1: Simplify template and add rotation logic**
Replace the dual-icon logic with a single icon and Tailwind animation classes.

```vue
<template>
  <AccordionHeader class="flex">
    <AccordionTrigger
      data-slot="accordion-trigger"
      v-bind="delegatedProps"
      :class="
        cn(
          'focus-visible:ring-ring/50 focus-visible:border-ring focus-visible:after:border-ring **:data-[slot=accordion-trigger-icon]:text-muted-foreground rounded-md py-4 text-left text-sm font-medium hover:underline focus-visible:ring-3 **:data-[slot=accordion-trigger-icon]:ml-auto **:data-[slot=accordion-trigger-icon]:size-4 group/accordion-trigger relative flex flex-1 items-start justify-between border border-transparent transition-all outline-none disabled:pointer-events-none disabled:opacity-50',
          props.class,
        )
      "
    >
      <slot />
      <slot name="icon">
        <Icon
          name="hugeicons:arrow-down-01"
          :size="16"
          data-slot="accordion-trigger-icon"
          class="pointer-events-none shrink-0 transition-transform duration-200 group-data-[state=open]/accordion-trigger:rotate-180"
        />
      </slot>
    </AccordionTrigger>
  </AccordionHeader>
</template>
```

- [ ] **Step 2: Commit changes**

```bash
git add app/components/ui/accordion/AccordionTrigger.vue
git commit -m "refactor(ui): use single rotating icon in AccordionTrigger"
```

---

### Task 2: Clean up Faq Component

**Files:**
- Modify: `app/components/landing/Faq.vue`

- [ ] **Step 1: Remove manual rotation class**
Remove the `[&[data-state=open]>svg]:rotate-180` class which now conflicts with the base component's logic.

```vue
<!-- Change line 39 from -->
<AccordionTrigger
  class="px-6 py-5 text-base md:text-lg font-semibold hover:no-underline text-left transition-all data-[state=open]:text-primary [&[data-state=open]>svg]:rotate-180"
>
<!-- to -->
<AccordionTrigger
  class="px-6 py-5 text-base md:text-lg font-semibold hover:no-underline text-left transition-all data-[state=open]:text-primary"
>
```

- [ ] **Step 2: Ensure vertical alignment**
Verify the `items-center` class on line 41 is present and correctly aligning the category icon and text.

- [ ] **Step 3: Commit changes**

```bash
git add app/components/landing/Faq.vue
git commit -m "fix(landing): clean up conflicting accordion styles in Faq"
```

---

### Task 3: Final Verification

- [ ] **Step 1: Verify landing page UI**
Check the FAQ section at `http://localhost:3000/#faq` (if running) or simply confirm the code changes reflect the spec.
- [ ] **Step 2: Check responsiveness**
Ensure the alignment looks good on both mobile and desktop sizes by checking the Tailwind classes in both files.
